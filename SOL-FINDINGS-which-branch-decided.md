# Which branch decided the live stall

All statements below are **MEASURED** unless explicitly marked **NOT ESTABLISHED**. I used an uncommitted scratch probe outside `tests/` to wrap `_calibration_town_key`, `_equipment_transaction_town_key`, `_town_special_key`, and `_town_blocked_key`, and drove retained snapshots through public `choose_key`. The accepted replay was required to reproduce the live key and `last_reason`; it reproduced all 26 retained surface decisions (turns 2940731--2940986) exactly as key `5`, reason `town:blocked:no-safe-recall-destination`. The cold-policy control diverged and was rejected as evidence. To reconstruct the live retained slice, the probe restored the state explicitly recorded at its boundary: town floor `(0,0,0)`, already in town, calibration phase `deposit`, and `_town_blocked_reason == "no-safe-recall-destination"`.

## Per-decision trace

| Live time / turn(s) | Live key | Producing call site | Calibration observation | Latch observation |
| --- | --- | --- | --- | --- |
| 12:03:29 / 2940680 (two decisions) | `ESC`, `ESC` | **NOT ESTABLISHED**: these Home-page inputs are not in `bot-state-fixed.jsonl`; the decision log says `home:leave-after-one-operation`, but that is not a wrapped execution trace. | **NOT ESTABLISHED** | **NOT ESTABLISHED** |
| 12:03:31 / 2940690 | `9` | **NOT ESTABLISHED**: the decision log names `town:blocked:no-safe-recall-destination`, but this decision predates the fixed retained slice. | **NOT ESTABLISHED** | **NOT ESTABLISHED**: this is the first logged blocked reason, but the exact assignment/return path was not observed by the accepted trace. |
| 12:03:33--12:03:36 / 2940704, 2940710, 2940720 | `5` x3 | **NOT ESTABLISHED**: these decisions predate the fixed retained slice. | **NOT ESTABLISHED** | The log repeats the blocked reason; exact call path **NOT ESTABLISHED**. |
| 12:03:37--12:04:15 / 2940731, 2940741, 2940745, 2940754, 2940772, 2940783, 2940792, 2940800, 2940813, 2940819, 2940833, 2940844, 2940854, 2940858, 2940873, 2940881, 2940889, 2940899, 2940912, 2940926, 2940937, 2940946, 2940959, 2940970, 2940981, 2940986 | `5` x26 | `_decide:4427` called `_town_special_key`; that called `_town_blocked_key` at `policy.py:18645`; `_town_blocked_key` returned `5`; `_town_special_key` returned it. The early `_town_blocked_key` call at `:3564` was not called. The `_town_special_key` call at `:4383` was not called. | Called at `_decide:4335` on every decision and returned `None` on every decision. Executed lines were `8637, 8638, 8639, 8642, 8643, 8664, 8665, 8675`: the `phase == "deposit"` path and its ordinary-routing `return None`. Immediately before every call, `_find_home_deposit(snapshot)` returned inventory slot `a`, 19 `テレポートの巻物` (`tval=70`, `sval=9`), not `None`. `_equipment_transaction_town_key` was then called at `:4343` and returned `None`. | `_town_blocked_reason` was `no-safe-recall-destination` before and after every decision. Nothing in the measured slice cleared it. The early condition at `:3561` was false because `_town_blocked_store_context(snapshot)` was false on these non-store snapshots; this is also directly evidenced by calibration being reached on every decision. |

## Owning branch

The stall owner is **MEASURED** for the complete retained fixed slice: `_town_special_key` at `_decide:4427`, specifically its already-latched blocked path at `policy.py:18645`, produces every repeated `5`. The proposed early `_town_blocked_key` call at `:3564` does not own these surface decisions, and calibration is not starved: it runs first on all 26, sees a real deposit candidate, and deliberately returns `None` so ordinary Home routing may handle it. The exact call that first established `no-safe-recall-destination` at turn 2940690 is **NOT ESTABLISHED** because `bot-state-fixed.jsonl` begins at turn 2940731; observing that predecessor with the live policy's pre-decision state (or a checkpoint that serializes it) would settle the onset path.

## Repair options

No repair option is offered. The trace makes branch ownership of the retained repeated decisions clear, but it does not establish why the latch was first installed, so proposing a repair would exceed the evidence.
