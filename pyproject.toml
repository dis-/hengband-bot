[project]
name = "hengbot"
version = "0.1.0"
description = "External bot client for Hengband bot JSON output"
requires-python = ">=3.10"
dependencies = ["numpy==2.3.5"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
