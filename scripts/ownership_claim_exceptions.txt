# Producers that S2a could not put inside one declared claim, with the reason.
#
# SOL-DESIGN-ownership-contract.md section 6 stage S2a, pin A2: every producer
# the ownership lint can see is inside a declared claim, or is listed here with
# a one-line reason.  The target is an empty file; every line below is work
# S2b/S3 still owes.
#
# Format: ``module.py:function  # reason``.  One function per line, the name as
# the module defines it at top level (a nested function is covered by its
# host).  ``scripts/ownership_claim_lint.py`` reads this file and reports the
# uncovered sites that are *not* excused; a stale line -- a function that no
# longer exists, or that has no uncovered site left -- is an error, so the file
# cannot rot into a blanket waiver.
#
# Two kinds of entry, and nothing else:
#
# 1. LADDER DISPATCHER.  The function is not a producer: it selects among the
#    producers of several owner families and writes the winner's reason itself.
#    One ``@claims`` would declare a single owner for sites that belong to up
#    to sixteen, so the marker these need is the other spelling of design 5.1
#    -- ``with self.claim(owner, goal):`` around each branch, each with its own
#    goal.  That is a declaration, not an annotation: it allocates a claim, so
#    it belongs to the stage that turns the marker into a declaration (S2b),
#    not to this classification round.
#
# 2. RELAY.  Every site assigns a reason computed elsewhere (a caller's
#    argument, a sub-producer's return, a stored terminal).  The owner is the
#    producer that built the string, so the relay cannot declare one without
#    inventing it; it is migrated with whatever it relays.

# -- 1. ladder dispatchers ---------------------------------------------------
policy.py:_decide  # the ladder itself: sixteen families' rungs, re-evaluated top-down every board (design 3.2)
policy.py:_choose_key  # the town/dungeon split above the ladder: nine families
policy.py:choose_key  # the post-decision rewrites of design 3.3.1, which are claims of their own (detectors, town-plan)
policy_shop.py:_shop  # in-store dispatcher: buy, sell, home, home-scan, survival and town-plan pages share one page reader
policy_quest.py:_quest_execute_key  # fixed-quest driver: quest-request travel and quest-sweep execution interleave per phase
policy_quest.py:_q2_phase_key  # Q2 phase machine: quest-request re-entry and quest-sweep phases
policy_quest.py:_q2_breach_key  # Q2 breach: quest-request equipment legs inside a quest-sweep phase
policy_quest.py:_opening_q34_town_key  # Q34 opening: detectors, quest-request and store-router legs in one town errand
policy_combat.py:_emergency_item  # consumable ladder shared by escape, survival and quest-sweep
policy_town.py:_town_special_key  # town special-case ladder: bookkeeping, departure, detectors, fundraising, rumor, survival
policy_town.py:_town_procurement_decision  # procurement rewrite of design 3.3.1: cross-town, detectors and town-plan
policy_town.py:_town_item_processing_key  # item triage: home-visit disposal and identification share the pack scan
policy_town.py:_town_blocked_key  # the blocked-reason overwrite: names the family it blocked
policy_town.py:_town_cancel_unsafe_recall_key  # cancels a departure claim on behalf of town-plan
policy_town.py:_town_destroy_key  # destruction gate: equipment-txn holdings and town-plan overflow
policy_town.py:_town_entrance_step_off_key  # departure leg wrapped by a detectors step-off
policy_home.py:_atomic_home_withdraw_key  # the atomic Home withdraw serves home-visit and equipment-txn callers
policy_home.py:_atomic_home_deposit_key  # the atomic Home deposit serves home-visit and equipment-txn callers
policy_home.py:_probe_unobserved_home_withdrawal  # home-visit probe that reports through a town-plan block
policy_home.py:_home_quest_launcher_key  # home-scan lookup feeding a home-visit withdrawal
policy_equipment.py:_home_rearm_key  # home-scan lookup feeding a home-visit rearm
policy_shop.py:_atomic_shop_transaction_key  # one atomic transaction for both shop-buy and shop-sell
policy_shop.py:_restore_mining_combat_hand_key  # relays the equipment-mutation report of whichever hand it restored
policy_supply.py:_wilderness_survival_key  # wilderness ladder: departure, store-router and survival
policy_supply.py:_mana_food_survival_override_key  # survival override that also writes the town-plan block it overrides
policy_calibration.py:_calibration_redress_key  # calibration redress that reports through a town-plan block
policy_calibration.py:_calibration_redress_observe  # the observation half of the same pair
policy_navigation.py:_step_toward  # shared stepper: the detectors oscillation break and the positioning re-route
policy_combat.py:_paralyzer_prevention_key  # positioning stand-off and the survival escalation behind it
policy.py:_darkness_recovery_key  # equipment-txn light swap or the survival fallback
policy.py:_bounty_cashout_key  # quest-request cash-out that travels as store-router
policy_observation.py:_resolve_observed_uncomposable_stop  # names the shop-buy operation inside a town-plan stop
policy.py:_recall_restock_key  # fundraising wait that relays the shop-buy restock reason

# -- 2. relays ---------------------------------------------------------------
policy.py:__init__  # initialises last_reason to design 4's idle default before any producer runs
policy.py:_destination_depth_allowed  # relays the depth-block reason its caller composed
policy.py:_unprofitable_unique_disengage_key  # relays the escape rung the disengage ladder chose
policy.py:_issue_emergency_consumable  # relays the emergency reason its caller passed
policy.py:_current_floor_item_key  # relays the floor-item reason of the loot producer it calls
policy.py:_claim_bar_gate  # relays back the reason a barred rung found (S2b.2 switch, off by default)
policy_calibration.py:_abort_character_calibration  # relays the abort reason its caller composed
policy_combat.py:_esp_threat_leave_key  # relays the departure reason the leave producer built
policy_equipment.py:_equipment_wield  # relays the equipment-mutation report of the wield it issued
policy_equipment.py:_equipment_takeoff  # relays the equipment-mutation report of the take-off it issued
policy_equipment.py:_wield_digging_tool_key  # relays the equipment-mutation report of the digger wield
policy_identification.py:_identify_carried_item_key  # relays the identification reason its caller passed
policy_quest.py:_fixed_quest_building_key  # relays the building-approach reason its candidate carries
policy_quest.py:_fixed_quest_building_candidate  # relays the candidate's own reason
policy_quest.py:_fixed_quest_building_unavailable_candidate  # relays the candidate's own reason
policy_shop.py:_publish_purchase_home_block  # relays the home-first block reason it publishes
policy_town.py:_town_travel_key  # relays the travel reason its caller composed
